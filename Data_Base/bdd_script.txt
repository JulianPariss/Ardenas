CREATE TABLE PLAYER (id number PRIMARY KEY auto_increment, name varchar(20), lastname varchar(20), nickname varchar(20), password varchar(20), 
phone_num varchar(20), email varchar(20));

CREATE TABLE GAME_ROLE (id number PRIMARY KEY  auto_increment , role_name varchar(20));

CREATE TABLE PLAYER_GAME_ROLE (id number PRIMARY KEY auto_increment,player_id number NOT NULL, game_role_id number NOT NULL);

CREATE TABLE MATCH_PLAYERS (id number PRIMARY KEY auto_increment,match_id number NOT NULL, players_id number NOT NULL);

CREATE TABLE MATCH(id number PRIMARY KEY auto_increment, fecha DATE);

DROP TABLE GAME_ROLE;
DROP TABLE PLAYER;
DROP TABLE PLAYER_GAME_ROLE;